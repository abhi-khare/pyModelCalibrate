
class bin:

    def __init__(self, prob, label) -> None:
        

class IsotonicRegressionCalibrator:

    def __init__(self, probs, labels):

        self.probs = probs
        self.labels = labels

        # check for size
        assert(len(probs) == len(labels))
    
    def fit()
